int zAlgorithm(string s, string p, int n, int m)
{
	 vector<int>v;
    int len=p.size();
    for(int i=0;i<s.size();i++){
      string temp=s.substr(i,len);
      if(temp==p) v.push_back(i+1);
  } return v.size();
}
