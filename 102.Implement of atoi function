#include <bits/stdc++.h> 
int atoi(string str) {
    // Write your code here.
    string s="";
    for(int i=0;i<str.size();i++){
        if(str[i]>='0' &&  str[i]<='9' || str[i]=='-') s=s+str[i];

    }
    return stoi(s);
}
